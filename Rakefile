desc "Build the site into the ./build directory"
task :build do
  sh "bundle exec jekyll build"
end

desc "Run a jekyll server for local development"
task :serve do
  sh "bundle exec jekyll server --watch"
end

